package com.pasindu.nursingotapp.ui.theme

import androidx.compose.ui.text.font.FontFamily

/**
 * Font families for the Nursing Super App design system.
 *
 * The project currently has no packaged font resources, so these aliases use
 * the Android system fallback until licensed font files are added to res/font.
 * Keeping the names centralized lets the typography system switch to the
 * bundled Poppins/Inter/Noto Sans Sinhala families without touching screens.
 */
val NursingHeadingFont = FontFamily.SansSerif
val NursingBodyFont = FontFamily.SansSerif
val NursingSinhalaFont = FontFamily.SansSerif
