<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Existing PDF sharing paths -->
    <cache-path name="shared_pdfs" path="pdf/" />
    <external-cache-path name="external_cache_pdfs" path="pdf/" />

    <!-- Paysheet camera capture: temporary files only -->
    <cache-path name="paysheet_camera" path="paysheet_camera/" />

    <!-- Existing app-private file sharing -->
    <files-path name="internal_files" path="." />
    <external-path name="external_files" path="." />
</paths>